<div class="crt-container">
  <div class="crt-tv dos-tv">
    <div class="crt-screen dos-screen">
      <div class="crt-bezel">
        <div id="dosEmulator" class="emulator-display">
          <iframe
            id="iaFrame"
            style="
              width: 100%;
              height: 100%;
              border: none;
              background: #000;
              display: none;
            "
            allow="fullscreen"
            referrerpolicy="no-referrer"
          ></iframe>
        </div>
        <div class="crt-scanlines dos-scanlines"></div>
        <div class="crt-glow dos-glow"></div>
      </div>
    </div>

    <div class="tv-controls">
      <div class="control-knob power-knob" data-action="power" title="Power">
        ⏻
      </div>
      <div class="control-knob volume-knob" data-action="volume" title="Volume">
        🔊
      </div>
      <div class="control-knob reset-knob" data-action="reset" title="Reset">
        ↺
      </div>
    </div>
  </div>

  <div class="game-library dos-library">
    <div class="library-header">💻</div>

    <div class="game-grid">
      <!-- Jeux DOS Internet Archive -->
      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Prince_of_Persia_1990"
        data-name="Prince of Persia"
      >
        <div class="cart-label">
          <div class="cart-icon">👑</div>
          <div class="cart-title">Prince of Persia</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Commander_Keen_1990"
        data-name="Commander Keen"
      >
        <div class="cart-label">
          <div class="cart-icon">🚀</div>
          <div class="cart-title">Commander Keen</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Wolfenstein_3D_1992"
        data-name="Wolfenstein 3D"
      >
        <div class="cart-label">
          <div class="cart-icon">🔫</div>
          <div class="cart-title">Wolfenstein 3D</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Doom_1993"
        data-name="DOOM"
      >
        <div class="cart-label">
          <div class="cart-icon">👹</div>
          <div class="cart-title">DOOM</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Civilization_1991"
        data-name="Civilization"
      >
        <div class="cart-label">
          <div class="cart-icon">🏛️</div>
          <div class="cart-title">Civilization</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_SimCity_1989"
        data-name="SimCity"
      >
        <div class="cart-label">
          <div class="cart-icon">🏙️</div>
          <div class="cart-title">SimCity</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Lemmings_1991"
        data-name="Lemmings"
      >
        <div class="cart-label">
          <div class="cart-icon">🐭</div>
          <div class="cart-title">Lemmings</div>
        </div>
      </div>

      <div
        class="game-cartridge dos-cart"
        data-ia="msdos_Monkey_Island_1990"
        data-name="Monkey Island"
      >
        <div class="cart-label">
          <div class="cart-icon">🏴‍☠️</div>
          <div class="cart-title">Monkey Island</div>
        </div>
      </div>
    </div>

    <div class="emulator-info">
      <p><strong>Émulateur:</strong> Internet Archive DOSBox</p>
      <p><strong>Formats:</strong> Jeux MS-DOS</p>
      <p><strong>Contrôles:</strong> Clavier + Souris</p>
    </div>
  </div>
</div>

<style>
  .crt-container {
    display: flex;
    gap: 40px;
    padding: 20px;
    min-height: 80vh;
    background: linear-gradient(135deg, #1a1a2e, #16213e);
  }

  .dos-tv {
    position: relative;
    background: linear-gradient(145deg, #34495e, #2c3e50);
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3),
      inset 0 2px 4px rgba(255, 255, 255, 0.1);
  }

  .dos-screen {
    position: relative;
    background: #000;
    border-radius: 15px;
    padding: 20px;
    box-shadow: inset 0 0 30px rgba(0, 0, 0, 0.8),
      0 0 20px rgba(255, 165, 0, 0.3);
  }

  .crt-bezel {
    position: relative;
    background: radial-gradient(circle at center, #111 0%, #000 100%);
    border-radius: 10px;
    overflow: hidden;
    width: 640px;
    height: 480px;
  }

  .emulator-display {
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, #1a1a2e, #ff6b00);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #ffa500;
    font-family: monospace;
    font-size: 24px;
    font-weight: bold;
    position: relative;
  }

  .emulator-display::before {
    content: "MS-DOS EMULATOR\AChoisissez un jeu →";
    white-space: pre;
    text-align: center;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 1;
  }

  .emulator-display.loaded::before {
    display: none;
  }

  .emulator-display.loaded iframe {
    display: block !important;
  }

  .dos-scanlines {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(255, 165, 0, 0.03) 2px,
      rgba(255, 165, 0, 0.03) 4px
    );
    pointer-events: none;
  }

  .dos-glow {
    position: absolute;
    top: -10px;
    left: -10px;
    right: -10px;
    bottom: -10px;
    background: radial-gradient(
      circle at center,
      rgba(255, 165, 0, 0.1) 0%,
      transparent 70%
    );
    pointer-events: none;
    animation: crt-glow 3s ease-in-out infinite alternate;
  }

  @keyframes crt-glow {
    0% {
      opacity: 0.8;
    }
    100% {
      opacity: 1;
    }
  }

  .tv-controls {
    position: absolute;
    right: 20px;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  .control-knob {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(145deg, #34495e, #2c3e50);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 16px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3),
      inset 0 2px 4px rgba(255, 255, 255, 0.1);
  }

  .control-knob:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.4),
      inset 0 2px 4px rgba(255, 255, 255, 0.2);
  }

  .control-knob:active {
    transform: scale(0.95);
  }

  .dos-library {
    flex: 1;
    background: linear-gradient(145deg, #34495e, #2c3e50);
    border-radius: 15px;
    padding: 20px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3),
      inset 0 2px 4px rgba(255, 255, 255, 0.1);
  }

  .library-header {
    text-align: center;
    font-size: 2em;
    margin-bottom: 20px;
    color: #ffa500;
  }

  .game-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 15px;
    margin-bottom: 20px;
  }

  .game-cartridge {
    background: linear-gradient(145deg, #ff6b00, #e55100);
    border-radius: 10px;
    padding: 15px;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2),
      inset 0 2px 4px rgba(255, 255, 255, 0.2);
  }

  .game-cartridge::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #ffa500, #ff6b00, #e55100, #bf360c);
  }

  .game-cartridge:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3), 0 0 20px rgba(255, 165, 0, 0.4);
  }

  .game-cartridge.active {
    background: linear-gradient(145deg, #ffa500, #ff8f00);
    color: white;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3), 0 0 30px rgba(255, 165, 0, 0.6);
  }

  .cart-label {
    text-align: center;
    color: white;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.7);
  }

  .cart-icon {
    font-size: 2em;
    margin-bottom: 5px;
  }

  .cart-title {
    font-size: 0.85em;
    font-weight: bold;
    line-height: 1.2;
  }

  .game-cartridge.active .cart-label {
    color: white;
  }

  .emulator-info {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 8px;
    padding: 15px;
    font-size: 0.9em;
    color: #ffa500;
  }

  .emulator-info p {
    margin: 5px 0;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .crt-container {
      flex-direction: column;
      gap: 20px;
    }

    .crt-bezel {
      width: 100%;
      max-width: 640px;
      height: auto;
      aspect-ratio: 4/3;
    }

    .tv-controls {
      position: static;
      flex-direction: row;
      justify-content: center;
      transform: none;
      margin-top: 20px;
    }

    .game-grid {
      grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    }
  }
</style>

<script>
  // Variables globales
  let currentGame = null;
  let gameFrame = null;

  // Initialisation
  document.addEventListener("DOMContentLoaded", () => {
    setupDOSEmulator();
    setupControls();
    setupGameLibrary();
  });

  function setupDOSEmulator() {
    console.log("💻 Initialisation émulateur MS-DOS...");

    // Préparer l'affichage
    const display = document.getElementById("dosEmulator");
    const iframe = document.getElementById("iaFrame");

    console.log("✅ Émulateur MS-DOS prêt");
  }

  function setupControls() {
    const controls = document.querySelectorAll(".control-knob");

    controls.forEach((control) => {
      control.addEventListener("click", () => {
        const action = control.dataset.action;

        switch (action) {
          case "power":
            togglePower();
            break;
          case "volume":
            toggleVolume();
            break;
          case "reset":
            resetEmulator();
            break;
        }
      });
    });
  }

  function setupGameLibrary() {
    const cartridges = document.querySelectorAll(".game-cartridge");

    cartridges.forEach((cartridge) => {
      cartridge.addEventListener("click", () => {
        const gameId = cartridge.dataset.ia;
        const gameName = cartridge.dataset.name;

        loadDOSGame(gameId, gameName);

        // Mise à jour visuelle
        cartridges.forEach((c) => c.classList.remove("active"));
        cartridge.classList.add("active");
      });
    });
  }

  function loadDOSGame(gameId, gameName) {
    console.log("💻 Chargement jeu DOS:", gameName);

    const display = document.getElementById("dosEmulator");
    const iframe = document.getElementById("iaFrame");

    // Construire l'URL Internet Archive
    const gameUrl = `https://archive.org/embed/${gameId}`;

    // Configurer et charger le jeu
    iframe.src = gameUrl;
    display.classList.add("loaded");

    currentGame = gameName;

    // Effet de chargement
    showLoadingEffect();

    console.log("🚀 Jeu chargé:", gameName);
  }

  function showLoadingEffect() {
    const scanlines = document.querySelector(".dos-scanlines");
    scanlines.style.animation = "loading-flicker 0.5s ease-in-out 3";
  }

  function togglePower() {
    const display = document.getElementById("dosEmulator");
    const iframe = document.getElementById("iaFrame");

    if (currentGame) {
      // Arrêter le jeu
      iframe.src = "";
      display.classList.remove("loaded");

      // Retirer la sélection active
      document.querySelectorAll(".game-cartridge").forEach((c) => {
        c.classList.remove("active");
      });

      currentGame = null;
      console.log("⏻ Émulateur éteint");
    }
  }

  function toggleVolume() {
    const volumeKnob = document.querySelector('[data-action="volume"]');
    volumeKnob.style.transform = "rotate(45deg)";

    setTimeout(() => {
      volumeKnob.style.transform = "";
    }, 200);

    console.log("🔊 Volume ajusté");
  }

  function resetEmulator() {
    if (currentGame) {
      const iframe = document.getElementById("iaFrame");
      iframe.src = iframe.src; // Recharger l'iframe
      showLoadingEffect();
      console.log("↺ Jeu redémarré");
    }
  }

  // Animation de chargement
  const style = document.createElement("style");
  style.textContent = `
    @keyframes loading-flicker {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }
  `;
  document.head.appendChild(style);

  console.log("💻 Page émulateur MS-DOS CRT chargée");
</script>
