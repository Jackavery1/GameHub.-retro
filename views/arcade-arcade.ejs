<div class="crt-container">
  <div class="crt-tv arcade-tv">
    <div class="crt-screen arcade-screen">
      <div class="crt-bezel">
        <canvas id="arcadeScreen" width="320" height="240"></canvas>
        <div class="crt-scanlines arcade-scanlines"></div>
        <div class="crt-glow arcade-glow"></div>
      </div>
    </div>

    <div class="tv-controls">
      <div class="control-knob power-knob" data-action="power">⏻</div>
      <div class="control-knob volume-knob" data-action="volume">🔊</div>
      <div class="control-knob channel-knob" data-action="reset">↺</div>
    </div>
  </div>

  <div class="game-library arcade-library">
    <div class="library-header">🕹️</div>
    <div class="game-grid">
      <!-- Jeux Arcade Internet Archive -->
      <div
        class="game-cartridge arcade-cart"
        data-game="pacman"
        data-url="https://archive.org/embed/arcade_pacman"
      >
        <div class="cart-label">
          <div class="cart-icon">👻</div>
          <div class="cart-title">Pac-Man</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="galaga"
        data-url="https://archive.org/embed/arcade_galaga"
      >
        <div class="cart-label">
          <div class="cart-icon">🚀</div>
          <div class="cart-title">Galaga</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="frogger"
        data-url="https://archive.org/embed/arcade_frogger"
      >
        <div class="cart-label">
          <div class="cart-icon">🐸</div>
          <div class="cart-title">Frogger</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="asteroids"
        data-url="https://archive.org/embed/arcade_asteroids"
      >
        <div class="cart-label">
          <div class="cart-icon">🪨</div>
          <div class="cart-title">Asteroids</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="centipede"
        data-url="https://archive.org/embed/arcade_centipede"
      >
        <div class="cart-label">
          <div class="cart-icon">🐛</div>
          <div class="cart-title">Centipede</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="donkey-kong"
        data-url="https://archive.org/embed/arcade_donkey_kong"
      >
        <div class="cart-label">
          <div class="cart-icon">🦍</div>
          <div class="cart-title">DK</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="space-invaders"
        data-url="https://archive.org/embed/arcade_space_invaders"
      >
        <div class="cart-label">
          <div class="cart-icon">👾</div>
          <div class="cart-title">Invaders</div>
        </div>
      </div>

      <div
        class="game-cartridge arcade-cart"
        data-game="street-fighter"
        data-url="https://archive.org/embed/arcade_street_fighter_2"
      >
        <div class="cart-label">
          <div class="cart-icon">👊</div>
          <div class="cart-title">Street F.</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .crt-container {
    display: flex;
    gap: 40px;
    padding: 20px;
    min-height: 80vh;
    background: linear-gradient(135deg, #8e44ad, #e74c3c);
    position: relative;
    overflow: hidden;
  }

  .crt-container::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(
        circle at 20% 50%,
        rgba(255, 255, 0, 0.1) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 80% 20%,
        rgba(255, 0, 255, 0.1) 0%,
        transparent 50%
      ),
      radial-gradient(
        circle at 40% 80%,
        rgba(0, 255, 255, 0.1) 0%,
        transparent 50%
      );
    animation: arcade-bg 10s ease-in-out infinite alternate;
  }

  @keyframes arcade-bg {
    0% {
      opacity: 0.3;
    }
    100% {
      opacity: 0.7;
    }
  }

  .arcade-tv {
    background: linear-gradient(145deg, #2c3e50, #34495e);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4),
      inset 0 2px 4px rgba(255, 255, 255, 0.1);
    z-index: 2;
  }

  .arcade-screen {
    background: #000;
    box-shadow: inset 0 0 30px rgba(0, 0, 0, 0.8),
      0 0 20px rgba(255, 255, 0, 0.4);
  }

  .arcade-glow {
    background: radial-gradient(
      circle at center,
      rgba(255, 255, 0, 0.2) 0%,
      transparent 70%
    );
  }

  #arcadeScreen {
    width: 640px;
    height: 480px;
    image-rendering: pixelated;
    border-radius: 8px;
    background: radial-gradient(circle at center, #ff00ff, #ffff00, #00ffff);
    display: block;
  }

  .arcade-library {
    background: linear-gradient(145deg, #2c3e50, #34495e);
    z-index: 2;
  }

  .arcade-cart {
    background: linear-gradient(145deg, #f39c12, #e67e22);
    animation: arcade-pulse 2s ease-in-out infinite alternate;
  }

  @keyframes arcade-pulse {
    0% {
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2),
        inset 0 2px 4px rgba(255, 255, 255, 0.2);
    }
    100% {
      box-shadow: 0 8px 16px rgba(243, 156, 18, 0.4),
        inset 0 2px 4px rgba(255, 255, 255, 0.3);
    }
  }

  .arcade-cart::before {
    background: linear-gradient(90deg, #ff00ff, #ffff00, #00ffff, #ff0000);
    animation: rainbow-border 3s linear infinite;
  }

  @keyframes rainbow-border {
    0% {
      background-position: 0% 50%;
    }
    100% {
      background-position: 100% 50%;
    }
  }

  .arcade-cart:hover {
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3), 0 0 20px rgba(255, 255, 0, 0.6);
    animation: none;
    transform: translateY(-5px) scale(1.02);
  }

  .arcade-cart.active {
    background: linear-gradient(145deg, #e74c3c, #c0392b);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3), 0 0 30px rgba(255, 255, 0, 0.8);
    animation: none;
  }

  .arcade-scanlines {
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 1px,
      rgba(255, 255, 0, 0.05) 1px,
      rgba(255, 255, 0, 0.05) 3px
    );
  }

  /* Styles communs réutilisés */
  .crt-tv {
    position: relative;
    border-radius: 20px;
    padding: 30px;
  }

  .crt-screen {
    position: relative;
    border-radius: 15px;
    padding: 20px;
  }

  .crt-bezel {
    position: relative;
    background: radial-gradient(circle at center, #111 0%, #000 100%);
    border-radius: 10px;
    overflow: hidden;
  }

  .crt-scanlines {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .crt-glow {
    position: absolute;
    top: -10px;
    left: -10px;
    right: -10px;
    bottom: -10px;
    pointer-events: none;
    animation: crt-glow 3s ease-in-out infinite alternate;
  }

  @keyframes crt-glow {
    0% {
      opacity: 0.8;
    }
    100% {
      opacity: 1;
    }
  }

  .tv-controls {
    position: absolute;
    right: 20px;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  .control-knob {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(145deg, #34495e, #2c3e50);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 16px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3),
      inset 0 2px 4px rgba(255, 255, 255, 0.1);
  }

  .control-knob:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.4),
      inset 0 2px 4px rgba(255, 255, 255, 0.2);
  }

  .control-knob:active {
    transform: scale(0.95);
  }

  .game-library {
    flex: 1;
    border-radius: 15px;
    padding: 20px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3),
      inset 0 2px 4px rgba(255, 255, 255, 0.1);
  }

  .library-header {
    text-align: center;
    font-size: 2em;
    margin-bottom: 20px;
    color: var(--neon);
    text-shadow: 0 0 10px var(--neon);
  }

  .game-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 15px;
  }

  .game-cartridge {
    border-radius: 10px;
    padding: 15px;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2),
      inset 0 2px 4px rgba(255, 255, 255, 0.2);
  }

  .game-cartridge:active {
    transform: translateY(-2px);
  }

  .game-cartridge::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background-size: 200% 100%;
  }

  .cart-label {
    text-align: center;
    color: white;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.5);
  }

  .cart-icon {
    font-size: 2em;
    margin-bottom: 5px;
  }

  .cart-title {
    font-size: 0.9em;
    font-weight: bold;
  }

  .game-cartridge.active .cart-label {
    color: white;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .crt-container {
      flex-direction: column;
      gap: 20px;
    }

    #arcadeScreen {
      width: 100%;
      max-width: 640px;
      height: auto;
    }

    .tv-controls {
      position: static;
      flex-direction: row;
      justify-content: center;
      transform: none;
      margin-top: 20px;
    }
  }
</style>

<script>
  // Gestion des jeux Arcade
  let currentGame = null;
  let gameFrame = null;

  // Initialisation
  document.addEventListener("DOMContentLoaded", () => {
    initializeEmulator();
    setupGameLibrary();
    setupControls();
  });

  function initializeEmulator() {
    const canvas = document.getElementById("arcadeScreen");
    const ctx = canvas.getContext("2d");

    // Affichage de démarrage Arcade avec effet psychédélique
    drawArcadeBackground(ctx);

    // Logo Arcade stylisé
    ctx.fillStyle = "#ffff00";
    ctx.font = "bold 32px monospace";
    ctx.textAlign = "center";
    ctx.strokeStyle = "#ff00ff";
    ctx.lineWidth = 2;
    ctx.fillText("ARCADE", canvas.width / 2, canvas.height / 2 - 20);
    ctx.strokeText("ARCADE", canvas.width / 2, canvas.height / 2 - 20);

    ctx.fillStyle = "#00ffff";
    ctx.font = "18px monospace";
    ctx.fillText(
      "Sélectionnez un jeu →",
      canvas.width / 2,
      canvas.height / 2 + 30
    );
  }

  function drawArcadeBackground(ctx) {
    // Fond coloré animé
    const gradient = ctx.createRadialGradient(
      canvas.width / 2,
      canvas.height / 2,
      0,
      canvas.width / 2,
      canvas.height / 2,
      Math.max(canvas.width, canvas.height) / 2
    );
    gradient.addColorStop(0, "#ff00ff");
    gradient.addColorStop(0.5, "#ffff00");
    gradient.addColorStop(1, "#00ffff");

    ctx.fillStyle = gradient;
    ctx.fillRect(0, 0, canvas.width, canvas.height);

    // Effet de grille
    ctx.strokeStyle = "rgba(255,255,255,0.1)";
    ctx.lineWidth = 1;

    for (let x = 0; x < canvas.width; x += 20) {
      ctx.beginPath();
      ctx.moveTo(x, 0);
      ctx.lineTo(x, canvas.height);
      ctx.stroke();
    }

    for (let y = 0; y < canvas.height; y += 20) {
      ctx.beginPath();
      ctx.moveTo(0, y);
      ctx.lineTo(canvas.width, y);
      ctx.stroke();
    }
  }

  function setupGameLibrary() {
    const cartridges = document.querySelectorAll(".game-cartridge");

    cartridges.forEach((cartridge) => {
      cartridge.addEventListener("click", () => {
        const gameUrl = cartridge.dataset.url;
        const gameName = cartridge.dataset.game;

        loadGame(gameUrl, gameName);

        // Mise à jour visuelle
        cartridges.forEach((c) => c.classList.remove("active"));
        cartridge.classList.add("active");
      });
    });
  }

  function setupControls() {
    const controls = document.querySelectorAll(".control-knob");

    controls.forEach((control) => {
      control.addEventListener("click", () => {
        const action = control.dataset.action;

        switch (action) {
          case "power":
            togglePower();
            break;
          case "volume":
            toggleVolume();
            break;
          case "reset":
            resetGame();
            break;
        }
      });
    });
  }

  function loadGame(gameUrl, gameName) {
    const canvas = document.getElementById("arcadeScreen");
    const container = canvas.parentElement;

    // Supprimer l'ancien iframe s'il existe
    if (gameFrame) {
      gameFrame.remove();
    }

    // Créer un nouveau iframe
    gameFrame = document.createElement("iframe");
    gameFrame.src = gameUrl;
    gameFrame.style.width = "100%";
    gameFrame.style.height = "100%";
    gameFrame.style.border = "none";
    gameFrame.style.borderRadius = "8px";
    gameFrame.style.position = "absolute";
    gameFrame.style.top = "0";
    gameFrame.style.left = "0";

    // Cacher le canvas et afficher l'iframe
    canvas.style.display = "none";
    container.style.position = "relative";
    container.appendChild(gameFrame);

    currentGame = gameName;

    // Effet de chargement
    showLoadingEffect();
  }

  function showLoadingEffect() {
    const scanlines = document.querySelector(".crt-scanlines");
    scanlines.style.animation = "loading-flicker 0.5s ease-in-out 3";
  }

  function togglePower() {
    const canvas = document.getElementById("arcadeScreen");
    const container = canvas.parentElement;

    if (gameFrame) {
      gameFrame.remove();
      gameFrame = null;
      canvas.style.display = "block";
      initializeEmulator();

      // Retirer la sélection active
      document.querySelectorAll(".game-cartridge").forEach((c) => {
        c.classList.remove("active");
      });
    }
  }

  function toggleVolume() {
    // Simulation du contrôle du volume
    const volumeKnob = document.querySelector('[data-action="volume"]');
    volumeKnob.style.transform = "rotate(45deg)";

    setTimeout(() => {
      volumeKnob.style.transform = "";
    }, 200);
  }

  function resetGame() {
    if (currentGame && gameFrame) {
      gameFrame.src = gameFrame.src; // Recharger l'iframe
      showLoadingEffect();
    }
  }

  // Animation de chargement
  const style = document.createElement("style");
  style.textContent = `
  @keyframes loading-flicker {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
`;
  document.head.appendChild(style);
</script>
